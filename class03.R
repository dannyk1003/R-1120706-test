people <- data.frame(
    name = c("Alice", "Bob", "Carol"),
    age = c(25, 30, 22),
    gerder = c("F", "M", "F"),
    city = c("USA", "UK", "TPE")
)

names <- people$name
ages <- people$age

print(names)
print(ages)